# AI Insights - Atualizacao de Conteudo

Edite `content.json` e depois rode:

```bash
node scripts/update-content.mjs
```

O script reconstrói os blocos de artigos, temas e ferramentas no `index.html`.
